<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>댓글 목록</h1>
    <a href="/">홈으로 이동하기</a>

    <ul>
        <% for (let i = 0; i < comments.length; i++ ) { %>
            <li> 
                <%= comments[i].userid %>
                -
                <!-- 반복문 i 가 0 이어서 id를 제대로 불러오지 못하기에 (i + 1) 을 해준다. -->
                <a href="/comment/<%= i + 1 %>">
                    <%= comments[i].comment %>
                </a>
            </li>
        <% } %>
    </ul>
</body>
</html>