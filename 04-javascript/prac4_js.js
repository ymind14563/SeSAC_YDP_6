const boyfreind = {
    age: 18,
    girlfreind: {name: `성춘향`, age: 16},
    isMarried: true,
    like: [`강아지`, `고양이`],
    name: `이몽룡`
}

console.log(boyfreind);


const mathScore = `77`;
const engScore = `88`;

const avgScore = (Number(mathScore) + Number(engScore)) / 2
console.log(avgScore, typeof(avgScore));